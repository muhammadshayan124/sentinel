services:
  sentinel:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./data:/app/data
      - sentinel_chroma:/app/chroma_data

volumes:
  sentinel_chroma:
